# get path to template
# get content and apply variables
# write it to sru.xml
# execure the run command to the exe file with install param
# ...\sru.exe install

from sru_service import (
    start,
    stop,
    install,
    uninstall,
    setup
)
    
